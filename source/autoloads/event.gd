extends Node

signal emit_audio(audio)
signal emit_ship_death(ship)
signal water_splash(xpos, strength)
signal spawn_parrot()
signal emit_parrot_death()
signal hug_update(amount, max_amount)
signal ship_update(amount)
signal hugging_update(is_hugging)
signal game_over()
signal spawn_new_broken_ship()
